{% extends 'base.html.twig' %}

{% block title %}Hello CourseController!{% endblock %}

{% block body %}
{# <h1>symfony_course</h1>
<table class="table table-striped table-hover">
    {% for row in courses %}

    <tr>
        <td> {{row.id}} {{row.title}} {{row.subject}} {{row.content}} {{row.published|date('d/m/Y')}}
        </td>
    </tr>
    {% endfor %}
</table> #}

<div class="row">
    {% for item in courses %}
    <div class="col-md-3">
        <h3>{{item.title}}</h3>
        <p>{{item.subject}}</p>
        <p>{{item.published|date('d/m/Y')}}</p>
        <a href="/courses/{{ item.id }}">See More</a>
    </div>
    {% endfor %}
</div>



{% endblock %}